[SPOTIFY]
CLIENT_ID={insert client id}
CLIENT_SECRET={insert client secret}
REDIRECT_URI={insert redirect uri}
SCOPE = user-library-read playlist-modify-public playlist-modify-private user-read-currently-playing user-modify-playback-state
USERNAME={insert username}
PLAYLIST={insert Discover playlist link}
ARCHIVE={insert Archive playlist link}

[GOOGLE]
PASSWORD={insert Gmail password}
SENDER={insert Gmail email address}
RECEIVER={insert recipient email address}
